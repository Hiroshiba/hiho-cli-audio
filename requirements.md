# PROJECT REQUIREMENTS - 音声文字起こしクリップボードツール（仮称）

作成日: 2026-06-27  
対象: 個人利用向け Electron デスクトップアプリ

---

## SOFTWARE TYPE

- デスクトップアプリケーション
- AI/API連携アプリケーション
- 常駐型ユーティリティ
- 対象OS: Windows・macOS
- 対象外OS: Linux

---

## PROJECT OVERVIEW

本アプリは、グローバルホットキーでマイク録音を開始・停止し、録音音声をGemini APIで日本語文字起こしして、完了後に自動でクリップボードへコピーする個人用デスクトップユーティリティである。

画面上の主UIは、録音中・認識中・完了・エラーだけを短時間表示する小型状態ウィンドウと、過去結果を再コピーするためデフォルトで表示される小型履歴ウィンドウに限定する。設定はアプリ内画面を持たず、YAML設定ファイルを直接編集する運用とする。

---

## PRODUCT VISION & GOALS

### 解決したい問題

- 音声入力・文字起こし結果を、すばやく任意のアプリへ貼り付けたい。
- 録音開始から文字起こし、クリップボードコピーまでの手動操作を最小化したい。
- 文字起こし用の大きなウィンドウや編集画面を常時表示せず、邪魔にならない形で使いたい。
- 個人利用前提で、設定画面やアカウント管理などの過剰な機能を避けたい。

### 成功状態

- ホットキーだけで録音開始・停止ができる。
- 録音停止後、Gemini APIによる文字起こしが実行され、結果が自動でクリップボードに入る。
- 完了したことだけが画面に短時間表示され、本文表示は不要。
- 過去の文字起こし結果を履歴からすばやく再コピーできる。
- アプリはWindows通知領域／macOSメニューバーに常駐し、必要以上に画面を占有しない。

### 失敗とみなす状態

- ホットキー操作が不安定で、録音開始・停止に失敗する。
- 文字起こし完了後にクリップボードコピーされていない。
- 小型ウィンドウが邪魔になる、または表示状態が分かりにくい。
- 履歴・音声ログの保存／削除ルールが不明瞭になる。
- 設定YAMLの変更が反映されない、または設定ミス時の挙動が分かりにくい。

---

## TARGET USERS

### 主ユーザー

- アプリ作成者本人のみ。
- WindowsおよびmacOSの両方で利用する。
- YAML設定ファイルを直接編集できる程度の技術理解がある。
- 音声入力の結果を、エディタ、チャット、ドキュメント、ブラウザなど任意のアプリへ貼り付ける用途を想定する。

### ユーザー種別

- 単一ユーザーのみ。
- 複数ユーザー、チーム利用、権限管理、アカウント管理は不要。

---

## CORE FEATURES

### Must Have

#### 1. 常駐アプリ

- アプリはWindowsでは通知領域、macOSではメニューバーに常駐する。
- ウィンドウを閉じても常駐する設計とする。
- OSログイン時の自動起動は初期版では行わない。
- メニューには最低限、履歴ウィンドウを前面に表示する項目と終了項目を置く。
- メニューから録音開始・停止は行わない。
- メニューから設定ファイルを開く項目は不要。

#### 2. グローバルホットキーによる録音トグル

- 録音開始・停止はグローバルホットキーのみで行う。
- 初期ホットキー:
  - Windows: `Ctrl + Shift + D`
  - macOS: `Command + Shift + D`
- ホットキーはYAML設定ファイルで変更可能にする。
- ホットキー登録に失敗した場合は、起動時にエラーを表示し、録音機能は待機状態にする。

#### 3. マイク録音

- OSの既定マイクを使用する。
- マイクデバイス選択機能は不要。
- 録音はWebM形式で一時保存または一時データとして扱う。
- 録音停止後、WebMをFFmpegで16kHzモノラルWAVへ変換する。
- 変換後、元のWebMは削除する。
- 音声ログとして保持するのは変換後の16kHzモノラルWAVのみ。

#### 4. FFmpeg同梱

- FFmpegはアプリに同梱する。
- ユーザーが別途FFmpegをインストールする必要はない。
- Windows・macOSそれぞれで利用可能な同梱方式を採用する。

#### 5. Gemini APIによる文字起こし

- 文字起こしにはGemini APIを使用する。
- Gemini APIキーはYAML設定ファイルに平文保存する。
- Geminiモデル名はYAMLで指定可能にする。
- モデルの初期値は実装時点で有効な音声入力対応モデルを採用する。
- 主な認識言語は日本語のみ。
- 文字起こし結果は、話した内容をできるだけそのまま出す方針とする。
- 文章化、要約、言い換え、フィラー除去、積極的な整形は行わない。

#### 6. カスタム語彙

- カスタム語彙はYAML設定ファイルに登録できる。
- 形式は「読み方 → 出力変換」の組を想定する。
- カスタム語彙はGeminiへのプロンプトに渡すだけとする。
- 文字起こし後の機械的な置換処理は行わない。

#### 7. 自動クリップボードコピー

- 文字起こし完了後、結果を自動でクリップボードへコピーする。
- 現在選択中の入力欄への自動貼り付けは行わない。
- 複数の文字起こしが並列に完了した場合、完了した順にクリップボードへコピーする。
- 後から完了した結果が、前の結果を上書きする。

#### 8. 並列文字起こし

- 前の文字起こし処理中でも、新しい録音を開始できる。
- 複数の録音が完了した場合、文字起こしは並列で実行する。
- 最大同時処理数のユーザー設定は設けない。
- 内部的な異常保護や例外処理は実装してよいが、ユーザーが設定する並列数は持たない。

#### 9. 自動停止タイマー

- 録音停止忘れ防止のため、自動停止タイマーを設ける。
- デフォルトは5分。
- 自動停止時間はYAML設定ファイルで変更可能にする。

#### 10. 小型状態ウィンドウ

- 小型状態ウィンドウは、待機中は非表示にする。
- 録音中は状態表示と録音時間をリアルタイム表示する。
- 認識中は処理中件数を表示する。例: `認識中: 2件`
- 録音中と認識中が同時に発生している場合は、録音中表示を優先する。
- 文字起こし完了時は本文を表示せず、コピーできたことが分かる表示のみ出す。
- 完了通知は3秒表示し、その後自動で非表示にする。
- 失敗時は小型状態ウィンドウにエラーを数秒表示し、その後自動で非表示にする。
- 小型状態ウィンドウはタイトルバー・最小化ボタンなしの枠なしウィンドウにする。
- ドラッグで移動可能にする。
- ウィンドウ位置は保存する。
- 初期表示位置は画面右上から少し下にする。

#### 11. 履歴機能

- 履歴画面は小さめ・横幅狭めのウィンドウにする。
- 履歴画面はアプリ起動時にデフォルトで表示する。
- 履歴画面を閉じた場合は、Windows通知領域／macOSメニューバーのメニューから再表示できる。
- 履歴表示用ホットキーは不要。
- 履歴には成功・失敗を合わせて最大10件を保持する。
- 件数上限はYAML設定ファイルで変更可能にする。
- 時間経過による自動削除は行わない。
- 履歴項目には日時と本文プレビューを表示する。
- 本文プレビューは全文ではなく数行分に抑える。
- 新しい文字起こしが完了した場合、履歴ウィンドウが表示中であれば一覧へ自動的に反映する。
- 履歴項目をクリックすると、成功項目の場合は該当文字起こし本文を即クリップボードへコピーする。
- コピー後、コピーできたことが分かる短時間の視覚的フィードバックを表示する。
- 履歴内で全文表示は行わない。
- 履歴本文の編集・上書き保存は行わない。
- 失敗項目は日時と「文字起こし失敗」を表示する。
- 失敗項目にエラー詳細は表示しない。
- 失敗項目をクリックしてもコピーは行わない。

#### 12. 音声ログ保存

- 録音音声は、履歴に紐づくログとしてローカル保存する。
- ユーザーに音声ファイルを明示的に提示しない。
- 履歴から音声再生はできない。
- 履歴から保存先フォルダを開く機能は不要。
- 履歴上限を超えて古い履歴が削除されるタイミングで、対応するWAV音声ログも削除する。
- 時間経過による音声ログ削除は不要。

#### 13. ウィンドウ最前面表示

- 小型状態ウィンドウと履歴ウィンドウの両方に最前面表示設定を適用する。
- デフォルトはON。
- 設定はYAML設定ファイルで変更可能にする。
- ウィンドウごとに表示位置を保存する。

#### 14. YAML設定ファイル

- 設定はアプリ内設定画面を作らず、YAML設定ファイルを直接編集する。
- YAML設定ファイルの変更はアプリ再起動時のみ反映する。
- 実行中の自動監視や再読み込みメニューは不要。
- Gemini APIキーもYAMLに平文保存する。
- 設定ファイルはElectronの標準的なユーザーデータ保存先、すなわち `app.getPath('userData')` 配下に置く。

### Should Have

#### 1. 設定バリデーション

- YAMLの構文エラーや必須項目不足がある場合、起動時に分かる形でエラー表示する。
- APIキー未設定、モデル未設定、無効なホットキー、無効な数値などは検出する。
- 設定エラー時は、録音開始などの危険な処理を行わない。

#### 2. 内部ログ

- 開発・トラブルシュート用に、最低限の内部ログをローカルに出力する。
- ログはユーザー向けUIには表示しない。
- 履歴画面にはエラー詳細を出さない。

#### 3. 安全なファイル書き込み

- 履歴・状態・設定初期生成などのファイルは、破損しにくい方式で書き込む。
- 履歴更新と音声ログ削除の整合性に注意する。

#### 4. 権限・初回起動対応

- 初回録音時にOSのマイク権限が必要になる場合、自然に権限要求へ進める。
- macOSで署名無しアプリの実行許可が必要な場合があることは、配布手順側で補足する。

### Could Have

初期版で必須ではないが、将来的に追加候補とする機能。

- 履歴検索。
- 履歴の手動削除。
- 文字起こし失敗時の詳細ログ参照。
- Gemini以外の文字起こしプロバイダ切り替え。
- 自動貼り付け。
- 音声ログフォルダを開く機能。
- 録音破棄操作。
- 起動時自動起動設定。

### Won't Have / Out of Scope

初期版では明確に作らないもの。

- Linux対応。
- アプリ内設定画面。
- 設定ファイルをメニューから開く機能。
- 実行中の設定再読み込み。
- OSログイン時の自動起動。
- 録音開始・停止のメニュー操作。
- 履歴表示用ホットキー。
- 入力欄への自動貼り付け。
- 最新文字起こし結果を1件だけ表示する専用ウィンドウ。
- 文字起こし本文の完了通知内表示。
- 履歴内での全文表示。
- 履歴本文の編集・保存。
- 履歴項目ごとのコピーボタン。クリック即コピーで代替する。
- 音声再生機能。
- 音声保存先フォルダを開く機能。
- Gemini失敗時の手動再試行。
- 誤録音の破棄操作。
- マイクデバイス選択機能。
- 並列文字起こし最大数のユーザー設定。
- 更新確認。
- 更新通知。
- 自動アップデート。
- GitHub Releases連携。
- 更新インストール。
- 更新案内ダイアログ。
- 署名・公証対応。
- ユーザーアカウント、ログイン、同期、クラウド履歴。
- チーム利用、権限管理、管理画面。

---

## TECHNICAL REQUIREMENTS

### 技術スタック

- Electron
- TypeScript推奨
- Node.jsランタイム
- Renderer側でMediaRecorderを使用してWebM音声を取得する想定
- Main Processで以下を担当する想定:
  - アプリ常駐制御
  - Tray/Menu Barメニュー
  - globalShortcut登録
  - BrowserWindow生成・位置保存
  - ファイル保存
  - FFmpeg実行
  - Gemini API呼び出し
  - クリップボード操作
  - 履歴管理

### プロセス構成案

#### Main Process

- アプリ起動・終了管理。
- YAML設定読み込み。
- グローバルホットキー登録。
- トレイ／メニューバー常駐。
- 状態ウィンドウ、履歴ウィンドウ、録音用レンダラーの制御。
- WebM一時ファイル受け取り。
- FFmpeg変換。
- Gemini API文字起こしジョブ実行。
- 文字起こし結果のクリップボードコピー。
- 履歴保存・削除。

#### Recorder Renderer

- `navigator.mediaDevices.getUserMedia()` でOS既定マイクから音声入力を取得。
- `MediaRecorder` でWebM形式の録音データを生成。
- 録音開始・停止はMain ProcessからIPCで指示を受ける。
- 録音終了後、WebMデータをMain Processへ渡す。

#### Status Renderer

- 小型状態ウィンドウの表示。
- 録音中、認識中、完了、エラーの状態表示。
- 録音時間のリアルタイム表示。
- ウィンドウドラッグ移動。

#### History Renderer

- 履歴一覧表示。
- 日時と数行プレビュー表示。
- 表示中は新しい履歴を自動的に反映する。
- 成功項目クリック時にMain Processへコピー依頼。
- コピー成功時は視覚的フィードバックを表示する。
- 失敗項目はコピー不可。

### データ保存場所

Electron標準のユーザーデータディレクトリである `app.getPath('userData')` 配下に保存する。

推奨構成:

```text
userData/
  config.yaml              # 手動編集する設定ファイル
  state.json               # ウィンドウ位置など自動管理する状態
  history.json             # 履歴メタデータ
  audio/
    <history-id>.wav       # 16kHz mono WAV音声ログ
  logs/
    app.log                # 内部ログ
  tmp/
    <recording-id>.webm    # 一時WebM。変換後削除
```

### 設定YAML案

```yaml
app:
  alwaysOnTop: true

hotkeys:
  toggleRecording:
    windows: "Control+Shift+D"
    macos: "Command+Shift+D"

recording:
  autoStopSeconds: 300

transcription:
  provider: "gemini"
  gemini:
    apiKey: ""
    model: ""
  language: "ja-JP"
  preserveSpeechAsMuchAsPossible: true

history:
  maxItems: 10

windows:
  status:
    initialPosition: "top-right-offset"
  history:
    narrow: true

vocabulary:
  - reading: "じぇみに"
    output: "Gemini"
```

補足:

- `transcription.gemini.model` が空の場合、実装時点で有効な音声入力対応Geminiモデルをデフォルトとして採用する。
- 実行中にYAMLを変更しても反映しない。反映は再起動時のみ。
- APIキーは平文保存でよい。
- APIキーは前後の空白文字を除去してから使用する。空白のみの場合は未設定として扱う。

### 音声変換仕様

入力:

- WebM音声。

変換:

- FFmpegで16kHz、モノラル、WAVへ変換。

概念的な変換コマンド:

```bash
ffmpeg -y -i input.webm -ac 1 -ar 16000 output.wav
```

保存:

- 保存する音声ログは `output.wav` のみ。
- `input.webm` は変換成功後に削除する。

### Gemini API仕様

- WAVファイルをGemini APIに送信して文字起こしする。
- プロンプトは日本語文字起こし専用にする。
- 発話内容をできるだけそのまま出力する。
- 要約、言い換え、文章整形、フィラー除去を避ける。
- カスタム語彙はプロンプトに含める。
- 機械的な後処理置換は行わない。

プロンプト方針の例:

```text
以下の日本語音声を文字起こししてください。
発話内容をできるだけそのまま文字にしてください。
要約、言い換え、補足、過度な整形、フィラー除去は避けてください。
句読点は必要最低限で構いません。

カスタム語彙がある場合は、可能な範囲で次の表記を優先してください。
- 読み: ... / 出力: ...
```

### クリップボード仕様

- 文字起こし成功時、結果全文をOSクリップボードへコピーする。
- コピー完了後、小型状態ウィンドウに完了表示を3秒出す。
- 並列処理時は完了順にコピーする。
- 最後に完了した結果がクリップボードの最終内容になる。
- 履歴の成功項目クリック時も、該当の全文をクリップボードへコピーする。

### 履歴仕様

履歴項目の想定データ:

```ts
type HistoryItem = {
  id: string;
  createdAt: string;
  completedAt: string | null;
  status: "success" | "failed";
  transcript: string | null;
  preview: string;
  audioPath: string | null;
};
```

表示:

- 新しい順に表示する。
- 成功項目: 日時 + 数行プレビュー。
- 失敗項目: 日時 + `文字起こし失敗`。
- エラー詳細は表示しない。
- 履歴ウィンドウはアプリ起動時にデフォルトで表示する。
- 文字起こし完了時、履歴ウィンドウが表示中であれば一覧へ自動的に反映する。

コピー:

- 成功項目クリック時、該当文字起こし全文をクリップボードへコピーする。
- コピー後、コピーできたことが分かる短時間の視覚的フィードバックを表示する。

削除:

- 成功・失敗を合わせて `history.maxItems` を超えた分を古い順に削除する。
- 履歴削除時、紐づくWAVファイルも削除する。

### 状態管理仕様

主要状態:

- idle
- recording
- converting
- transcribing
- completed
- failed
- hotkeyError

表示優先順位:

1. 録音中
2. 認識中
3. 完了通知
4. エラー通知
5. 待機中は非表示

録音中:

- `録音中 00:12` のように経過時間を表示する。
- 自動停止までの残り時間表示は必須ではない。

認識中:

- 複数件ある場合は `認識中: 2件` のように件数表示する。

完了:

- `クリップボードにコピーしました` など、コピーできたことが分かる表示のみ。
- 本文は出さない。
- 3秒で非表示。

失敗:

- `文字起こしに失敗しました` などの簡潔な表示。
- 数秒で非表示。
- 詳細エラーは履歴には出さず、内部ログにのみ残す。

### OS・配布要件

- Windows・macOS対応。
- Linux対応なし。
- Electronおよびビルドツールの標準サポート範囲で、最新のWindows・macOSで動くことを目標にする。
- 古いOS向けの追加実装は行わない。
- 初期版は署名なし。
- macOS署名・公証なし。
- Windowsコード署名なし。
- 更新機能は一切実装しない。
- 必要に応じて手動で新しいビルドへ入れ替える運用とする。

### セキュリティ・プライバシー

- 個人利用前提。
- Gemini APIキーはYAMLに平文保存する。
- ローカル履歴と音声ログは暗号化しない前提。
- 音声データは文字起こしのためGemini APIへ送信される。
- チーム利用や第三者配布を前提としたセキュリティ対策は初期スコープ外。
- ただし、不要な外部送信や分析SDKは入れない。

### パフォーマンス要件

- ホットキー押下から録音開始までの体感遅延を小さくする。
- 録音中の時間表示は1秒程度の更新でよい。
- 録音停止後、変換・Gemini API送信・コピー処理は非同期で実行する。
- 並列文字起こし中も、新しい録音開始を妨げない。
- 個人利用のため、大規模データや多数ユーザー向けの最適化は不要。

### 信頼性要件

- 録音中に自動停止タイマーが確実に作動する。
- Gemini API失敗時、アプリ全体が落ちない。
- FFmpeg変換失敗時、履歴に失敗として記録し、状態ウィンドウで簡潔に通知する。
- ホットキー競合時、起動時に分かる形で通知する。
- 履歴ファイル破損時には、可能ならバックアップまたは安全な初期化を行う。

---

## DESIGN REQUIREMENTS

### 全体方針

- 個人利用向けの実用性重視。
- 大きなメインウィンドウを持たない。
- 操作はホットキーと履歴メニューに絞る。
- 表示は最小限にする。
- 文字起こし本文を積極的に画面表示しない。

### 小型状態ウィンドウ

- 画面右上から少し下に初期表示。
- 枠なし、タイトルバーなし、最小化ボタンなし。
- ドラッグ移動可能。
- 位置保存あり。
- 最前面表示デフォルトON。
- 待機中は非表示。
- 録音・認識・完了・エラーの短い状態表示のみ。

### 履歴ウィンドウ

- 横幅狭めの小型ウィンドウ。
- アプリ起動時にデフォルトで表示する。
- 最前面表示デフォルトON。
- 位置保存あり。
- 日時と数行プレビューを一覧表示。
- 表示中は新しい履歴を自動的に反映する。
- 成功項目はクリックで即コピーし、コピーできたことが分かる視覚的フィードバックを表示する。
- 失敗項目はエラーだったことだけ分かる表示。
- 全文表示、編集、音声再生、保存先表示は行わない。

---

## USER EXPERIENCE FLOWS

### 1. 通常の録音・文字起こしフロー

1. ユーザーがグローバルホットキーを押す。
2. アプリがOS既定マイクで録音を開始する。
3. 小型状態ウィンドウが表示され、録音中状態と経過時間を表示する。
4. ユーザーが再度グローバルホットキーを押す。
5. 録音を停止する。
6. WebM音声を一時生成する。
7. FFmpegで16kHzモノラルWAVへ変換する。
8. WebMを削除し、WAVを音声ログとして保存する。
9. Gemini APIへWAVを送信し、日本語文字起こしを行う。
10. 成功したら結果全文をクリップボードへ自動コピーする。
11. 小型状態ウィンドウに「クリップボードにコピーしました」相当の表示を3秒出す。
12. 履歴に成功項目として日時と数行プレビューを追加し、履歴ウィンドウが表示中であれば一覧へ自動的に反映する。
13. 小型状態ウィンドウを自動で非表示にする。

### 2. 録音の自動停止フロー

1. ユーザーが録音開始後、停止操作を忘れる。
2. 設定された自動停止時間に到達する。
3. アプリが録音を自動停止する。
4. 以降は通常の変換・文字起こし・コピー処理へ進む。

### 3. 並列文字起こしフロー

1. 1件目の録音停止後、Gemini API処理が始まる。
2. 処理中にユーザーがホットキーを押して新しい録音を開始する。
3. 小型状態ウィンドウは録音中表示を優先する。
4. 2件目の録音停止後、2件目も並列で文字起こし処理を開始する。
5. 認識中のみの状態では `認識中: 2件` のように件数を表示する。
6. 完了した順にクリップボードへコピーする。
7. 最後に完了した結果がクリップボードの最終内容になる。

### 4. 履歴から再コピーするフロー

1. 小型履歴ウィンドウはアプリ起動時からデフォルトで表示されており、日時と数行プレビューが並んでいる。
2. ウィンドウを閉じていた場合は、Windows通知領域／macOSメニューバーのメニューから再表示できる。
3. ユーザーが成功履歴項目をクリックする。
4. アプリが該当文字起こし全文をクリップボードへコピーする。
5. コピーできたことが分かる短時間の視覚的フィードバックを表示する。
6. 本文の全文表示や編集画面は開かない。

### 5. 文字起こし失敗フロー

1. Gemini API、FFmpeg、ネットワーク、権限などの問題で処理に失敗する。
2. 小型状態ウィンドウに簡潔なエラーを数秒表示する。
3. 履歴に日時と「文字起こし失敗」を記録する。
4. エラー詳細は履歴に表示しない。
5. 手動再試行機能は提供しない。

### 6. ホットキー登録失敗フロー

1. アプリ起動時にホットキー登録を試行する。
2. 他アプリとの競合などで登録に失敗する。
3. 起動時にエラー表示する。
4. 録音機能は待機状態にする。
5. ユーザーはYAMLのホットキー設定を変更し、アプリを再起動して反映する。

---

## CONSTRAINTS

### Budget

- 未定。
- 個人利用向けのため、低運用コストを前提とする。
- Gemini API利用料金はユーザー自身のAPIキーに紐づく。

### Timeline

- 未定。
- まず初期版として、ホットキー録音、Gemini文字起こし、自動コピー、状態表示、履歴、YAML設定を優先する。

### Team

- 個人利用・個人開発想定。
- 外部ユーザーサポート、管理者、承認者は不要。

### Technical

- Electronで実装する。
- Windows・macOS対応。
- Linux不要。
- FFmpeg同梱。
- Gemini API使用。
- YAML設定ファイル運用。
- 更新機能なし。
- 署名なし。
- 最新OSで動けばよく、古いOS向けの追加実装はしない。

---

## SUCCESS METRICS

個人利用アプリのため、外部分析や利用統計は不要。成功判定は主に体感品質と安定性で行う。

- ホットキー録音開始・停止の成功率。
- 録音停止後にクリップボードへ結果が入るまでの体感速度。
- Gemini文字起こし精度。
- 文字起こし結果が「話した内容をできるだけそのまま」になっているか。
- 小型状態ウィンドウが邪魔にならないか。
- 履歴から過去結果をすばやく再コピーできるか。
- 履歴上限と音声ログ削除が期待どおりに動くか。
- Windows・macOS双方で常駐、ホットキー、録音、クリップボードが安定するか。

---

## POST-LAUNCH

### 保守

- 個人利用前提で、手動で修正・再ビルドする。
- 自動更新・更新通知・リリース通知は行わない。
- 新しいバージョンを使う場合は手動で入れ替える。

### サポート

- 外部ユーザーサポート不要。
- 問題調査用に内部ログをローカルに残す程度でよい。

### 将来拡張

- 必要になった場合のみ、履歴検索、手動削除、プロバイダ切り替え、自動貼り付けなどを検討する。
- 初期版ではスコープ外とする。

---

## OUT OF SCOPE

以下は初期版では実装しない。

- Linux対応。
- 複数ユーザー対応。
- アカウント作成・ログイン。
- クラウド同期。
- 自動アップデート。
- 更新確認・更新通知。
- GitHub Releases連携。
- 署名・公証。
- 自動起動。
- メニューからの録音操作。
- 設定画面。
- 設定ファイルを開くメニュー。
- 設定のホットリロード。
- 自動貼り付け。
- 最新結果専用ウィンドウ。
- 結果本文の完了通知表示。
- 履歴の全文表示。
- 履歴編集。
- 履歴検索。
- 履歴からの音声再生。
- 音声フォルダを開く機能。
- Gemini失敗時の手動再試行。
- 録音破棄。
- マイク選択。
- 音や通知音によるフィードバック。
- 並列処理数のユーザー設定。
- エラー詳細の履歴表示。

---

## OPEN QUESTIONS

現時点で仕様確定に必須の未決事項はなし。

実装時に決めればよい事項:

- アプリ名。
- アイコン。
- 小型状態ウィンドウの具体的な幅・高さ・フォントサイズ。
- 履歴ウィンドウの具体的な幅・高さ。
- 内部ログのローテーション方式。
- Gemini APIクライアント実装の具体的なSDK・HTTP方式。
- 初期Geminiモデル名。
- ビルド成果物形式。例: Windows NSIS/portable、macOS dmg/zipなど。

---

## NOTES & INSIGHTS

- UIは「文字起こしエディタ」ではなく「ホットキー駆動のクリップボード投入ツール」として設計する。
- 状態表示は必要最小限にし、本文は基本的に画面に出さない。
- 履歴は編集・閲覧ではなく、過去結果の再コピーを主目的とする。
- 音声ログはユーザーに見せるためではなく、いざとなれば掘り起こせるローカル記録として保存する。
- 設定はGUIではなくYAMLに集約するため、設定バリデーションとエラーメッセージの分かりやすさが重要である。
- 途中で検討した更新関連機能は撤回され、完全に不要となった。
- 個人利用に最適化し、配布・サポート・署名・アップデート・アカウント管理などの製品化コストは避ける。
